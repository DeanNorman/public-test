// All these values are in pixels
// They are left in number format in order to do math in them if so required
export const appHeaderHeight = 56;
export const borderRadiusDefault = 4;
export const navWidth = 240;
export const navToggleWidth = 60;
export const navBreakpoint = 992;
export const mobileBreakpoint = 480;

export default {
   appHeaderHeight,
   borderRadiusDefault,
   navWidth,
   navToggleWidth,
   navBreakpoint,
   mobileBreakpoint,
};
